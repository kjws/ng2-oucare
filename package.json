{
  "name": "ng2-oucare",
  "version": "0.1.0",
  "description": "",
  "main": "./dist/index.js",
  "peerDependencies": {
    "typescript": ">=1.8",
    "typings": ">=1.3",
    "@angular/core": "2.0.0-rc.4",
    "rxjs": "5.0.0-beta.6",
    "zone.js": "^0.6.6",
    "lodash": "^4.13.1"
  },
  "scripts": {
    "install": "rimraf dist && typings install && tsc && npm run copy",
    "copy": "npm run copy-jade && npm run copy-less",
    "copy-jade": "copyfiles -u 1 ./src/**/*.jade dist",
    "copy-less": "copyfiles -u 1 ./src/**/*.less dist"
  },
  "typings": "./dist/index.d.ts",
  "author": "OUcare.com",
  "dependencies": {},
  "devDependencies": {
    "copyfiles": "^1.0.0",
    "rimraf": "^2.5.3"
  }
}